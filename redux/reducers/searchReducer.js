const initialState = {
   term: '',
   location: '',
   results: [],
   searhQueries: [],
   fetching: false
};

export default function (state = initialState, action) {
   switch (action) {
      case 'value': {
         break;
      }

      default:
         break;
   }
}
