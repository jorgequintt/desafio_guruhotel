const initialState = {
   allBusinesses: {},
   activeBusiness: null
};

export default function (state = initialState, action) {
   switch (action) {
      case 'value': {
         break;
      }

      default:
         break;
   }
}
