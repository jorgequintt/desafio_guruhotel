/**
 * * User types term and location and hits search
 * * Search bar is disable while fetching results
 * * UI symbol indicating "searching"
 * * Search results are fetched and displayed
 * * User can click on a business to see more details about it
 *  * User can see working hours, prices, and stuff
 *  * User can go back to search results
 * * User will see an eye icon on already seen results (even between searches)
 */

/*
    Store
        data
            - business: array
            - searchQueries: array
        search
            - results: array
            - term: string
            - location: string
            - fetching: bool
        


 */
